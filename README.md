# HACK-X
🚀 HACK‑X — Auto‑Restart Terminal Animation Script  HACK‑X is an advanced Python terminal program featuring dynamic ASCII animation, flame effects, wave motion, screen shake, glowing title, audio playback, and a full auto‑restart system. The script runs continuously without requiring the user to execute commands repeatedly.
